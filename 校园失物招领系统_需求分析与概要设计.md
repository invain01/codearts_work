# “拾光”校园失物招领系统
## 需求分析与概要设计（简化版 · 单机运行）

> 选题说明：换一个与商城无关的题材，做成**单体 B/S 应用**，全部在本机运行。不使用云服务器、容器、消息队列、缓存等外部资源，版本管理用本地 Git 即可，作为软件工程实训的轻量题目。

---

## 一、项目背景与定位

### 1.1 业务背景
校园内物品遗失与拾获信息分散在微信群、表白墙、公告栏，查找效率低、认领无凭证、物品长期无人认领。系统提供**失物登记、招领登记、检索匹配、认领审核**的集中管理，帮助失主快速找回物品。

### 1.2 项目定位
| 项 | 说明 |
|---|---|
| 形态 | 单体 B/S 应用，前后端一体，浏览器访问 |
| 规模 | 6 个功能模块、8 张数据表 |
| 运行 | 单机本地运行，零外部服务依赖 |
| 课程覆盖 | 数据库、操作系统（文件/进程）、计算机网络（HTTP）、数据结构（检索）、软件工程 |
| 与凤凰商城差异 | 不用 ECS/CodeArts 云资源，不做微服务/容器，本地起服务即用 |

---

## 二、需求分析

### 2.1 用户角色
| 角色 | 说明 | 主要操作 |
|---|---|---|
| 游客 | 未登录 | 浏览、检索失物/招领信息 |
| 注册用户 | 登录后的师生 | 发布失物/招领、申请认领、查看通知 |
| 管理员 | 平台维护 | 分类管理、审核信息、处理认领、查看统计 |

### 2.2 功能性需求（6 个模块）

**M1 用户模块**
- 注册 / 登录 / 退出（密码加盐哈希，会话保持）
- 个人资料与联系方式维护

**M2 失物登记模块**
- 发布丢失物品：名称、分类、丢失时间、地点、描述、图片（可选）
- 修改 / 删除自己的记录、标记“已找回”

**M3 招领登记模块**
- 发布拾获物品：名称、分类、拾获时间、地点、描述、图片（可选）
- 标记“已归还”

**M4 检索与匹配模块**
- 按关键词、分类、地点、时间范围筛选，分页展示
- 发布失物时自动提示可能匹配的招领记录（名称/分类/地点相近）

**M5 认领申请与审核模块**
- 用户对招领记录提交认领申请（说明特征）
- 发布者或管理员确认，通过后物品标记已归还，并通知申请人

**M6 后台管理模块**
- 分类维护、信息审核与下架
- 简单统计：发布量、认领成功率、分类分布

### 2.3 非功能性需求
| 类别 | 指标 |
|---|---|
| 性能 | 单机支持 50 并发，列表响应 < 300ms |
| 易用 | 界面简洁，主要操作 ≤3 步 |
| 安全 | 密码哈希存储、登录鉴权、越权校验、参数化查询 |
| 可维护 | 单体分层清晰，配置集中，数据可备份导出 |
| 部署 | 一条命令启动，无需安装数据库服务 |

### 2.4 主要用例
- UC-01 发布失物：登录 → 填写信息 → 保存 → 系统提示匹配项
- UC-02 检索物品：输入关键词/筛选 → 查看列表 → 打开详情
- UC-03 认领审核：提交申请 → 发布者/管理员确认 → 物品归还并通知
- UC-04 后台管理：管理员审核信息、维护分类、查看统计

---

## 三、概要设计

### 3.1 总体架构（单体三层）

```
       浏览器（游客 / 用户 / 管理员）
                 │ HTTP
        ┌────────▼─────────┐
        │   表现层          │  页面模板 + 静态资源（Bootstrap）
        ├──────────────────┤
        │   业务层          │  用户/失物/招领/检索/认领/管理 逻辑
        ├──────────────────┤
        │   数据访问层      │  统一 ORM 访问
        └────────┬─────────┘
                 │
           ┌─────▼─────┐
           │ SQLite 文件库 │  单文件 app.db，随项目目录
           └───────────┘
```

### 3.2 技术选型（尽量少依赖）
| 层次 | 选型 |
|---|---|
| 后端 | Python 3.11 + Flask |
| 模板/前端 | Jinja2 + Bootstrap 5（服务端渲染，无前端构建） |
| 数据库 | SQLite（文件数据库，免安装） |
| ORM | Flask-SQLAlchemy |
| 鉴权 | Flask 会话 / 简单 Token |
| 版本管理 | 本地 Git（不上云） |
| 运行 | `python app.py`，访问 `http://127.0.0.1:5000` |

> 备选：若要求前后端分离，可改为 Spring Boot + MySQL，但依赖更重，本期不采用。

### 3.3 模块与目录结构
```
lost-and-found/
├─ app.py                 # 入口与路由注册
├─ config.py              # 配置（数据库路径、密钥）
├─ models.py              # 数据模型
├─ services/              # 业务逻辑（user/item/match/claim/admin）
├─ templates/             # Jinja2 页面
├─ static/                # css/js/上传图片
├─ requirements.txt
└─ app.db                 # SQLite 数据文件（运行生成）
```

### 3.4 数据库设计（8 张表）
| # | 表名 | 说明 | 关键字段 |
|---|---|---|---|
| 1 | t_user | 用户 | id, username, password_hash, role, contact |
| 2 | t_category | 物品分类 | id, name, sort |
| 3 | t_lost_item | 失物登记 | id, user_id, category_id, title, lost_time, place, status |
| 4 | t_found_item | 招领登记 | id, user_id, category_id, title, found_time, place, status |
| 5 | t_item_image | 图片 | id, item_type, item_id, path |
| 6 | t_claim | 认领申请 | id, found_item_id, applicant_id, feature_desc, status, auditor_id |
| 7 | t_notification | 通知 | id, user_id, content, is_read, create_time |
| 8 | t_audit_log | 操作日志 | id, operator_id, action, target, create_time |

主要关系：`t_user 1—N t_lost_item/t_found_item`、`t_found_item 1—N t_claim`、`t_category 1—N 物品`。

### 3.5 关键接口（路由概要）
| 方法 | 路径 | 说明 |
|---|---|---|
| GET/POST | /register /login /logout | 注册、登录、退出 |
| GET | /items | 失物/招领列表与筛选 |
| POST | /lost | 发布失物（返回匹配提示） |
| POST | /found | 发布招领 |
| GET | /item/{id} | 详情 |
| POST | /claim | 提交认领申请 |
| POST | /claim/{id}/audit | 审核认领 |
| GET | /admin | 后台管理与统计 |

### 3.6 运行与部署（单机、无云资源）
1. 安装依赖：`pip install -r requirements.txt`
2. 初始化数据库：首次启动自动建表，或 `flask init-db`
3. 启动：`python app.py`，浏览器访问 `http://127.0.0.1:5000`
4. 数据备份：直接复制 `app.db` 文件
5. 版本管理：本地 `git init/commit`，如需可推送校内 GitLab（非必须）

### 3.7 安全与测试
- 安全：密码 BCrypt 哈希、登录态校验、按 `user_id` 做越权校验、ORM 参数化防注入。
- 测试：单元测试（业务逻辑/匹配算法）+ 接口测试（注册登录、发布、认领流程）+ 手工界面测试。

---

## 四、开发计划（3 周，6~8 人）

| 周次 | 目标 | 交付物 |
|---|---|---|
| 第 1 周 | 需求分析、概要设计、数据库与界面设计 | 需求说明、概要设计、ER 图、汇报 PPT |
| 第 2 周 | 完成核心模块编码（用户/失物/招领/检索） | 可运行原型、接口说明、周报 |
| 第 3 周 | 完成认领/管理/统计与测试，整理文档 | 测试报告、用户手册、实验报告、答辩 PPT |

角色分工：项目经理、需求/设计、后端、前端、测试、文档（每人一管理+一技术角色）。

---

## 五、简化取舍说明（YAGNI）
- 不做微服务、容器、消息队列、缓存、对象存储 → 单体 + SQLite 足够。
- 不做真实短信/邮件、支付、地图定位 → 用站内通知、文本地点代替。
- 不做前后端分离与前端构建 → 服务端渲染最省事。
- 不上云 → 全部本机运行，答辩现场直接演示。
